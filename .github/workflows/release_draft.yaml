name: "Release Drafter"

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - edited

jobs:
  run:
    name: "Release Drafter: Run"
    permissions:
      contents: write
      pull-requests: read
    runs-on: ubuntu-latest
    steps:
      - name: "Repository: Checkout"
        uses: actions/checkout@v6.0.2

      - name: "Release Drafter: Draft"
        uses: release-drafter/release-drafter@v7.0.0
        with:
          config-name: configurations/release_draft.yaml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
